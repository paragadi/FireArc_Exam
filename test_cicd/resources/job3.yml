resources:
  jobs:
    Job3:
      name: Job3
      tasks:
        - task_key: hob4
          notebook_task:
            notebook_path: ${workspace.root_path}/notebooks/TFK/1-Bronze/test_notebook
            source: WORKSPACE
            base_parameters:
                env: ${bundle.target}
                uc_catalog: ${var.uc_catalog}
                uc_schema: ${var.uc_schema}
                table_name: ${var.table_name}